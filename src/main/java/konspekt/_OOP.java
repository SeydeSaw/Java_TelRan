package konspekt;
/*
    ООП (Объектно-ориентированное программирование) - это один из основных принципов Java
    (объектно-ориентированный язык программирования).
    Позволяет структурировать программы вокруг объектов, которые являются экземплярами классов.
    Основными концепциями ООП являются классы и объекты.

    OOP (Objektorientierte Programmierung) ist ein grundlegendes Prinzip von Java
    (eine objektorientierte Programmiersprache).
    Es ermöglicht die Strukturierung von Programmen um Objekte, die Instanzen von Klassen sind.
    Die Hauptkonzepte in der OOP sind Klassen und Objekte.

    Класс - это шаблон или модель, описывающая состояние (поля) и поведение (методы) объекта.
    Klasse: Dies ist eine Vorlage oder ein Modell, das den Zustand (Felder) und das Verhalten (Methoden)
    eines Objekts beschreibt.

    Объект - это экземпляр класса, созданный с использованием оператора "new".
    Objekt: Dies ist eine Instanz einer Klasse, erstellt mit dem "new"-Operator.

    Некоторые ключевые концепции ООП, которые применяются в Java:
    Einige wichtige OOP-Konzepte, die in Java verwendet werden:

		Инкапсуляция (Encapsulation): Сокрытие реализации
		Идея заключается в объединении данных и методов, работающих с ними, в классе.
		Класс предоставляет интерфейс, через который другие объекты могут взаимодействовать с его данными
		и выполнять определенные операции.

		Kapselung (Encapsulation): Verbergen der Implementierung.
		Die Idee besteht darin, Daten und die Methoden, die mit ihnen arbeiten, in einer Klasse zu vereinen.
		Die Klasse stellt eine Schnittstelle bereit, über die andere Objekte mit ihren Daten interagieren
		und bestimmte Operationen ausführen können.

		Наследование (Inheritance): Создание новой сущности на базе уже существующей.
		Одним из преимуществ ООП является возможность создания иерархии классов, где
		классы-потомки наследуют свойства и методы от классов-предков.
		В Java используется ключевое слово "extends" для указания наследования.

		Vererbung (Inheritance): Schaffung einer neuen Entität auf Basis einer bereits vorhandenen.
		Ein Vorteil der OOP besteht darin, dass hierarchische Klassenstrukturen erstellt werden können,
		in denen abgeleitete Klassen Eigenschaften und Methoden von Elternklassen erben.
		In Java wird das Schlüsselwort "extends" für die Vererbung verwendet.

		Полиморфизм (Polymorphism): Возможность иметь разные формы для одной и той же сущности.
		Полиморфизм позволяет объектам разных классов иметь общий интерфейс
		и вести себя по-разному в зависимости от контекста.
		Достигается с помощью переопределения методов и использования интерфейсов.

		Polymorphismus (Polymorphism): Die Möglichkeit, verschiedene Formen für dieselbe Entität zu haben.
		Polymorphismus ermöglicht es Objekten unterschiedlicher Klassen, eine gemeinsame Schnittstelle zu haben
		und sich je nach Kontext unterschiedlich zu verhalten.
		Er wird durch die Methode des Überschreibens und die Verwendung von Schnittstellen erreicht.

		Абстракция (Abstraction): Набор общих характеристик
		Абстракция позволяет сосредоточиться на существенных аспектах объекта и скрыть ненужные
		детали реализации. Достигается с помощью абстрактных классов и интерфейсов.

		Abstraktion (Abstraction): Eine Sammlung gemeinsamer Eigenschaften.
		Abstraktion ermöglicht es, sich auf wesentliche Aspekte eines Objekts zu konzentrieren und
		unnötige Implementierungsdetails zu verbergen. Dies wird durch abstrakte Klassen und Schnittstellen erreicht.

		Посылка сообщений: Форма связи, взаимодействия между сущностями.
		Nachrichtenübermittlung: Eine Form der Kommunikation, Interaktion zwischen Entitäten.

		Переиспользование: Всё что перечислено выше работает на повторное использование кода.
		Wiederverwendung: Alles, was oben aufgeführt ist, funktioniert für die Wiederverwendung von Code.

		Классы и объекты:
		Классы используются для определения состояния и поведения объектов.
		Они содержат поля (переменные) для хранения данных и методы для выполнения операций.
		Объекты - это экземпляры классов, созданные во время выполнения программы.

		Klassen und Objekte: Klassen werden verwendet, um den Zustand und das Verhalten von Objekten zu definieren.
		Sie enthalten Felder (Variablen) zum Speichern von Daten und Methoden zum Ausführen von Operationen.
		Objekte sind Instanzen von Klassen, die während der Laufzeit des Programms erstellt werden.

		Модификаторы доступа:
		Java предоставляет модификаторы доступа (например, public, private, protected),
		которые управляют видимостью полей и методов класса для других классов и объектов.

		Zugriffsmodifikatoren: Java bietet Zugriffsmodifikatoren (z.B. public, private, protected),
		die die Sichtbarkeit von Feldern und Methoden einer Klasse für andere Klassen und Objekte steuern.

		Конструкторы:
		Конструкторы используются для создания объектов класса.
		Они инициализируют поля объекта при его создании.
		В Java конструкторы имеют имя, совпадающее с именем класса.

		Konstruktoren: Konstruktoren werden verwendet, um Objekte einer Klasse zu erstellen.
		Sie initialisieren die Felder eines Objekts bei seiner Erstellung.
		In Java haben Konstruktoren denselben Namen wie die Klasse.

 */
public class _OOP {
}