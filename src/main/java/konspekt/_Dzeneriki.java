package konspekt;
/*
    Дженерики - это механизм, позволяющий создавать классы, интерфейсы и методы,
    которые могут работать с различными типами данных.
    Если мы делаем класс обобщённым, который имеет параметр типа <Т>,
    то параметр типа <Т> может быть любым типом данных,
    указанном при создании экземпляра класса.

    Generics sind ein Mechanismus, der es ermöglicht, Klassen, Schnittstellen und Methoden zu erstellen,
    die mit verschiedenen Datentypen arbeiten können.
    Wenn wir eine Klasse generisch machen, die einen Typenparameter <T> hat,
    kann dieser Typenparameter <T> jeder Datentyp sein,
    der beim Erstellen einer Instanz der Klasse angegeben wird.

 */
public class _Dzeneriki {
    /*
    public class Box<T> {
    private T value;

    public void setValue(T value) {
        this.value = value;
    }

    public T getValue() {
        return value;
    }
}

    // Использование обобщенного класса
    Box<Integer> integerBox = new Box<>();
    integerBox.setValue(42);
    int value = integerBox.getValue();
    System.out.println(value);
     */
}
