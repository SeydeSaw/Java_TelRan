package konspekt;

public class _this_super {
    /*
    this():
    В двух словах, this() вызывает конструктор в том же классе,
    а super() вызывает конструктор родительского класса.

    In zwei Worten, this() ruft den Konstruktor in derselben Klasse auf,
    während super() den Konstruktor der Elternklasse aufruft.

    Что это: this() используется для вызова конструктора того же класса.
    Когда используется: Когда у класса есть несколько конструкторов,
    и вы хотите вызвать один из них из другого конструктора в том же классе.

    Was ist das: this() wird verwendet, um den Konstruktor derselben Klasse aufzurufen.
    Wann wird es verwendet: Wenn eine Klasse mehrere Konstruktoren hat
    und Sie einen von ihnen aus einem anderen Konstruktor derselben Klasse aufrufen möchten.

    super():

    Что это: super() используется для вызова конструктора родительского класса.
    Когда используется: Когда у вас есть наследование (родительский и дочерний класс),
    и вы хотите вызвать конструктор родительского класса из конструктора дочернего класса.

    Was ist das: super() wird verwendet, um den Konstruktor der Elternklasse aufzurufen.
    Wann wird es verwendet: Wenn Sie Vererbung haben (eine Eltern- und eine Kinderklasse)
    und Sie den Konstruktor der Elternklasse aus dem Konstruktor der Kinderklasse aufrufen möchten.
     */
}