package konspekt;
/*
Отличия:
Unterschiede:

Примитивные
-хранят в памяти значение самого типа
-не имеют методов и представляют только значение
-имеют фиксированный размер в памяти (int 4 байта, boolean 1 байт)
-присваюваются значениями (int x = 5;)
-при сравнении используется оператор == для сравнения значений

Primitive Datentypen:
-Speichern den Wert des Typs im Speicher.
-Haben keine Methoden und repräsentieren nur den Wert.
-Haben eine feste Größe im Speicher (int 4 Byte, boolean 1 Byte).
-Werden durch Werte zugewiesen (int x = 5;).
-Bei Vergleichen wird der Operator == verwendet, um die Werte zu vergleichen.

Ссылочные
-хранят ссылки(адрес) на объект в памяти
-представляют объекты, которые могут иметь состояние и методы, позволяющие взаимодействовать с этим состоянием
-имеют размер, зависящий от платформы и JVM, обычно занимают больше места
-присваиваются ссылками на объекы(String str = new String("Hello");
-при сравнении оператор == сравнивает ссылки,т.е. проверяет указывают ли две переменные на один и тот же объет в памяти.
для сравнения значений объектов используется мето equals, например для сравнения содержимого строк, а не их ссылки
(возвращает true или false)

Referenzdatentypen:
-Speichern Referenzen (Adresse) auf Objekte im Speicher.
-Repräsentieren Objekte, die einen Zustand und Methoden haben, um mit diesem Zustand zu interagieren.
-Haben eine Größe, die von der Plattform und der JVM abhängt und normalerweise mehr Platz einnimmt.
-Werden mit Referenzen auf Objekte zugewiesen (String str = new String("Hallo");).
-Bei Vergleichen vergleicht der Operator == die Referenzen, d. h. er prüft, ob zwei Variablen auf
 dasselbe Objekt im Speicher verweisen.
-Für den Vergleich von Objektwerten wird die Methode equals verwendet, z. B. zum Vergleich von Zeichenketteninhalten
anstelle ihrer Referenzen (gibt true oder false zurück).

 */
public class _Tipq_Dannqh_Ssqlochnqe_Primitivnqe {
}
